import React from "react"
import classes from './Header.module.scss'

export const Header = () => {
    return(
        <div className={classes.appHeader}>
            <h1>Header</h1>
        </div>
    )
}